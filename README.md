# 符号旅游系统 (Viajes FH)

邮轮旅游业务管理系统，支持账单生成、报价管理、票据跟踪和价格对比分析。

## 功能特性

- 🧾 **账单模式** - B2B内部账单，完整佣金计算和净价结算
- 💰 **报价模式** - B2C客户报价，隐藏敏感信息
- 🎫 **票据模式** - 支付凭证，跟踪已付/待付金额
- 📊 **对比模式** - 西区/德区价格对比，会员折扣和利润分析
- 🌍 **多语言支持** - 中文、西班牙语、英语
- ☁️ **实时同步** - Firebase 云端数据同步
- 🖨️ **打印支持** - A4 格式打印输出

## 技术栈

- **前端**: 原生 HTML5/CSS3/JavaScript (ES6+)
- **后端**: Firebase Realtime Database
- **认证**: Firebase Authentication
- **样式**: CSS 变量 + 模块化 CSS

## 项目结构

```
viajes-fh-main/
├── index.html              # 应用入口
├── css/
│   ├── style.css           # 主样式文件
│   ├── bill-mode.css       # 账单模式样式
│   ├── quote-mode.css      # 报价模式样式
│   ├── ticket-mode.css     # 票据模式样式
│   ├── compare-mode.css    # 对比模式样式
│   └── login.css           # 登录页面样式
├── js/
│   ├── core/               # 核心模块
│   │   ├── firebase-config.js  # Firebase 配置
│   │   ├── auth.js         # 认证模块
│   │   ├── i18n.js         # 国际化
│   │   └── utils.js        # 工具函数
│   ├── modes/              # 模式模块
│   │   ├── bill-mode.js
│   │   ├── quote-mode.js
│   │   ├── ticket-mode.js
│   │   └── compare-mode.js
│   └── main.js             # 应用主逻辑
└── docs/                   # 文档
```

## 快速开始

1. 使用支持 ES6 模块的浏览器打开 `index.html`
2. 输入密码登录系统
3. 选择需要的模式（账单/报价/票据/对比）
4. 填写相关信息并生成文档

> ⚠️ 需要网络连接以访问 Firebase 服务

## 数据存储

所有配置和业务数据存储在 Firebase Realtime Database：
- `/modes/{mode}/settings` - 各模式配置（客户、船名、航线等）
- `/modes/{mode}/draft` - 当前草稿数据

## 开发说明

### 添加新语言

编辑 `js/core/i18n.js`，在 `translations` 对象中添加新的语言配置。

### 自定义样式

- 全局样式修改 → `css/style.css`
- 模式专属样式 → `css/{mode}-mode.css`
- 登录页面样式 → `css/login.css`

### 调试

如需启用控制台日志，取消 `index.html` 中 `security.js` 的注释。

## 许可证

私有项目 - FH GLOBAL S.L.
